# 引き継ぎメモ（このアプリについて）

このファイルは、**次にこのアプリを触る人（または別のAI）が最初に読む用**のメモです。
専門知識がなくても読めるように書いてあります。

---

## 1. これは何のアプリ？

バドミントンの試合の組み合わせを自動で決めるアプリです。

- 参加人数（4〜40名）とコート数（1〜10面）を入れて「試合開始」を押す
- 誰と誰が組んで、どのコートで戦うかを自動で割り振る
- 勝ったペアを選んで記録していく
- 同じ人が連続で出続けないように、休憩が回るよう調整される

スマホのブラウザで開いて使います。ホーム画面に追加するとアプリのように使えます（PWA）。

---

## 2. いちばん大事なこと：インストール作業が要りません

このアプリは **ファイルを置くだけで動きます**。

- npm install みたいな準備は**不要**
- ビルド（変換作業）も**不要**
- 外部のライブラリを一切使っていない（React も jQuery も無し）

なので `index.html` をダブルクリックすればその場で開けます。
**この身軽さがこのアプリの一番の資産なので、なるべく壊さないでください。**
新しいライブラリを足したくなったときは、本当に必要か一度立ち止まってください。

---

## 3. ファイルの地図（どれを直せばいいか）

| ファイル | 中身 | こういう時に開く |
|---|---|---|
| `index.html` | 画面の**骨組み**。ボタンや文字の配置 | 「ボタンを増やしたい」「表示する項目を変えたい」 |
| `styles.css` | **見た目**。色・大きさ・余白 | 「色を変えたい」「文字を大きくしたい」「レイアウトを直したい」 |
| `app.js` | **頭脳**。組み合わせを決める計算、勝敗の記録 | 「ペアの決め方を変えたい」「休憩のルールを変えたい」「バグを直したい」 |
| `sw.js` | オフラインで動かすための仕組み | 基本さわらない（→ 5章の注意点だけ読んで） |
| `manifest.json` | アプリ名やアイコンの設定 | 「アプリ名を変えたい」 |

**もともとは全部 `index.html` の1ファイルに入っていました**が、他の人・他のAIが扱いやすいように3つに分けてあります。中身のコードは1行も変えていません。

---

## 4. 動かし方・公開のしかた

### 手元で確認する

`index.html` をブラウザで開くだけです。
（オフライン機能まで含めて正しく試したい場合は、フォルダの中で
`python3 -m http.server 8000` を実行して `http://localhost:8000` を開いてください）

### みんなが使えるように公開する

`master` ブランチに push すると GitHub Pages で公開されます。
URL は `https://enakagami98-lang.github.io/badminton-match-maker/` です。
（もし表示されない場合は GitHub の Settings → Pages で公開設定を確認してください）

---

## 5. 【重要】ファイルを増やしたら必ずやること

`sw.js` の中に、こう書かれた場所があります。

```js
const CACHE = 'bm-v2';
const FILES = ['./index.html', './styles.css', './app.js', './manifest.json'];
```

- `FILES` は「オフライン用に保存しておくファイルの一覧」です。
  **新しいファイルを追加したら、ここにも書き足してください。** 書き忘れると、そのファイルだけオフラインで読めません。
- `CACHE` の `bm-v2` は**バージョン番号**です。
  **ファイルを増やしたり減らしたりしたら、`bm-v3`, `bm-v4` … と数字を上げてください。**
  上げ忘れると、前に使ったことがある人のスマホが**古いままの画面を表示し続けます**（「直したはずなのに直ってない！」の原因はほぼこれです）。

---

## 6. 分かっている不具合

**アプリのアイコン画像がありません。**

`manifest.json` が `icon-192.png` と `icon-512.png` を指していますが、この2つのファイルは存在しません。
そのため「ホーム画面に追加」したときのアイコンが正しく出ません。

直すには、192×192ピクセルと512×512ピクセルのPNG画像を作って、
このフォルダに `icon-192.png` / `icon-512.png` という名前で置いてください。
置いたあと、**5章のとおり `sw.js` の `FILES` に追加して `CACHE` の番号を上げてください。**

---

## 7. 別のAIに手伝ってもらうときのコツ

ChatGPT や Gemini などに頼むときは、**直したい内容に応じて必要なファイルだけ貼ってください。**
全部貼ると長すぎて、AIが途中を勝手に省略したり、関係ない場所を壊したりします。

| やりたいこと | 貼るファイル |
|---|---|
| 色や見た目を変える | `styles.css` だけ |
| ボタンや表示項目を足す | `index.html` だけ |
| 組み合わせのルールを変える／バグを直す | `app.js` だけ |

そして、こう伝えると事故が減ります。

> 「変更した部分だけを教えてください。ファイル全体を書き直さないでください。」
> 「外部のライブラリは追加しないでください。」

**ファイルを直接編集できるタイプのAIツール**（Claude Code、GitHub Copilot、Gemini CLI など）を使う場合は、コピペ不要でそのまま「ここを直して」と頼めます。

---

## 8. 今の作りのメモ（技術的な話・読み飛ばし可）

- 素の HTML / CSS / JavaScript のみ。フレームワークもビルドツールも無し
- `app.js` の関数は**グローバル**に定義されています。`index.html` の中の
  `onclick="startGame()"` のような書き方がそれを直接呼んでいるためです。
  **`app.js` を「モジュール形式」に変えると、この呼び出しが全部動かなくなります。** 変えるなら `onclick` も全部書き換えが必要です
- 状態は `app.js` 冒頭の `state` というオブジェクトに集約されています。まずここを読むと全体像がつかめます
- 連続出場を防ぐルールは過去に修正済みです（コミット `cf6c435`）。ペアの決め方をいじるときは、この挙動を壊していないか確認してください
